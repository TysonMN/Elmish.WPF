<Window x:Class="Elmish.WPF.Samples.SubModelSeq.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elmish.WPF.Samples.SubModelSeq"
        Title="SubModelSeq"
        Height="800"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
  <StackPanel Margin="0,15,0,0">
    <ComboBox ItemsSource="{Binding FirstStep}" SelectedItem="{Binding SelectedFirstStep, Mode=TwoWay}"/>
    <DataGrid ItemsSource="{Binding Entities}" SelectedItem="{Binding SelectedEntityId, Mode=TwoWay}" AutoGenerateColumns="False">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Some Field" Binding="{Binding SomeField, Mode=TwoWay}"></DataGridTextColumn>
      </DataGrid.Columns>
    </DataGrid>
  </StackPanel>
</Window>
